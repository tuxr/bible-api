{
  "name": "bible-api",
  "version": "1.0.0",
  "description": "Self-hosted Bible API on Cloudflare Workers with D1",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "data:download": "tsx data/scripts/download-sources.ts",
    "data:parse": "tsx data/scripts/parse-usfx.ts",
    "data:validate": "tsx data/scripts/validate.ts",
    "data:derive-versification": "tsx data/scripts/derive-versification.ts",
    "db:schema:local": "wrangler d1 execute bible-db --local --file=./schemas/schema.sql",
    "db:schema": "wrangler d1 execute bible-db --file=./schemas/schema.sql",
    "db:seed": "tsx data/scripts/seed-d1.ts",
    "db:migrate:text-plain": "tsx data/scripts/migrate-text-plain.ts",
    "db:migrate:segments": "tsx data/scripts/migrate-segments.ts",
    "db:backfill:segments": "tsx data/scripts/backfill-segments.ts"
  },
  "dependencies": {
    "hono": "^4.12.34"
  },
  "overrides": {
    "esbuild": "0.28.1"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.16.17",
    "@cloudflare/workers-types": "^4.20241127.0",
    "@types/sax": "^1.2.7",
    "@types/unzipper": "^0.10.10",
    "sax": "^1.4.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "unzipper": "^0.12.0",
    "vitest": "^4.1.9",
    "wrangler": "^4.102.0"
  }
}
